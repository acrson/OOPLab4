import javax.swing.*;
import java.awt.*;

public class ChartPanel extends JPanel {
    public ChartPanel() {
        setPreferredSize(new Dimension(545, 400));
        setBackground(Color.LIGHT_GRAY);
    }
}
